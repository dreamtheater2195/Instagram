$("#like_<%= @post.id %>").removeClass('glyphicon-heart').removeClass('like').addClass('glyphicon-heart-empty');
$("#like_<%= @post.id %>").attr("href", '/posts/<%= @post.id %>/like');
$("#likes_<%= @post.id %>").html("<%= j (render partial: 'posts/likes', locals: { post: @post} ) %>");